/*WAP to input side of a square and find its area */
#include<stdio.h>
void main()
{
	float side;
	
	printf("\n enter a side of a square : ");
	scanf("%f",&side);
	
	printf("\n area of a square is %.4f",side*side);
	
}